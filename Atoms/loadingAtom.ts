import {atom} from "recoil";

export const myLoadingState = atom({
    key: 'myLoadingState',
    default: "false"


});
export const myLoaderState = atom({
    key: 'myLoaderState',
    default: false


});